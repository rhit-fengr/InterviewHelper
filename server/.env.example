# Server environment variables
# Copy this file to .env and fill in your values

# AI provider selection: openai | gemini
AI_PROVIDER=openai

# OpenAI API Key (required when AI_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here

# Gemini API Key (required when AI_PROVIDER=gemini)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional model overrides
OPENAI_MODEL=gpt-4o
GEMINI_MODEL=gemini-2.0-flash
GEMINI_FALLBACK_MODELS=gemini-1.5-flash
AI_RATE_LIMIT_COOLDOWN_MS=60000

# Optional local STT endpoint (for local Whisper/faster-whisper service)
# Auto transcription fallback order is: openai -> local -> gemini
# Example expected contract: POST multipart/form-data with field `audio`, return {"text":"..."}
# LOCAL_TRANSCRIBE_URL=http://127.0.0.1:8765/transcribe

# Stripe (required for billing features)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PRICE_ID=price_your_monthly_price_id
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Server port (default: 4000)
PORT=4000

# Allowed CORS origins (comma-separated or * for all; restrict in production)
# Example: CORS_ORIGIN=https://your-app.com
CORS_ORIGIN=*
